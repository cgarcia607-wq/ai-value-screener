"""Placeholder smoke test so the first CI run passes green.

Replace with real tests as modules are built — every data client and
feature transformer needs at least a smoke test per CLAUDE.md.
"""


def test_smoke() -> None:
    assert True
